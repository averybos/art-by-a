{
  "dependencies": {
    "express": "^4.17.1",
    "react-scripts": "4.0.3",
    "react": "^16.2.0",
    "react-dom": "^16.2.0",
    "react-router-dom": "^5.2.0"
  },
  "devDependencies": {
    "concurrently": "^6.2.0"
  },
  "scripts": {
    "start": "npm run client",
    "client": "cd client && npm start",
    "build": "cd client && npm run build"
  }
}
